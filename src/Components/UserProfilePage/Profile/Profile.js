import React from 'react';
import UserPosts from '../UserPosts/UserPosts';

const Profile = () => {
    return (
        <div>
            <UserPosts></UserPosts>
        </div>
    );
};

export default Profile;