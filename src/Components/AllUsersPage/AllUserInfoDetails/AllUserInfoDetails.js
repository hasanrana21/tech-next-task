import React from 'react';

const AllUserInfoDetails = (props) => {
    console.log(props.usersPosts);
    const {} = props.usersPosts; // akan teke start krte hobe just bosai dewa.
    return (
        <div>
            
        </div>
    );
};

export default AllUserInfoDetails;